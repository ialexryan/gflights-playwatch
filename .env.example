# Slack incoming webhook URL (optional). If empty, output prints to console only.
SLACK_WEBHOOK=

# Dollar threshold for alerting (applies to flight-specific rows). Default 25.
PRICE_DROP_THRESHOLD=25
